/*
import '../../live_player_controller.dart';
import 'live_video_selector.dart';

class LiveVideoSelectorController extends LivePlayerChildController {
  LiveVideoSelectorState state;

  void show(){
    state?.show();
  }
  void hide(){
    state?.hide();
  }
  bool get isShowed{
    // TODO: 重构注释
    return false;
    //return state?.isShowed ?? false;
  }

  @override
  void setPlayer(LivePlayerController player){
    super.setPlayer(player);
  }
}
 */

