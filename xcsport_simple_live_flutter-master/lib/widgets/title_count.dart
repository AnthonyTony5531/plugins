

import 'package:flutter/material.dart';

class TitleCount extends StatelessWidget {
  final String title;
  final String count; 
  final bool selected;
  const TitleCount({Key key, this.title = "", this.count = "", this.selected = false}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      // child: ,
    );
  }
}

